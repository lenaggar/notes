
- `k create namespace mealie`
```bash
$ k create namespace mealie
namespace/mealie created
```

- `k get namespaces`
```bash
$ k get namespaces 
NAME              STATUS   AGE
default           Active   6d7h
kube-node-lease   Active   6d7h
kube-public       Active   6d7h
kube-system       Active   6d7h
mealie            Active   5s
```

- `k describe namespaces mealie`
```bash
$ k describe namespaces mealie 
Name:         mealie
Labels:       kubernetes.io/metadata.name=mealie
Annotations:  <none>
Status:       Active

No resource quota.

No LimitRange resource.
```

- `k create namespace mealie -o yaml --dry-run=client`
```bash
$ k describe namespaces mealie 
Name:         mealie
Labels:       kubernetes.io/metadata.name=mealie
Annotations:  <none>
Status:       Active

No resource quota.

No LimitRange resource.
```

- `k run nginx-mealie --image=nginx --namespace=mealie`
```bash
$ k run nginx-mealie --image=nginx --namespace=mealie
pod/nginx-mealie created
```

- `k config set-context --current --namespace=mealie`
```bash
$ k config set-context --current --namespace=mealie
Context "homelab" modified.
```

- `k config get-contexts`
```bash
$ k config get-contexts 
CURRENT   NAME              CLUSTER           AUTHINFO          NAMESPACE
*         homelab           homelab           homelab           mealie
          rancher-desktop   rancher-desktop   rancher-desktop   
```
